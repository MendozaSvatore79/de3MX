// Importamos el slide completo de Figma
import HeroSlide4 from '../imports/HeroSlide4';

// Este componente simplemente renderiza el slide de Figma
// La rotación se maneja en el componente padre (HeroCarousel)
export default function Slide4DigitalWrapper() {
  return <HeroSlide4 />;
}
